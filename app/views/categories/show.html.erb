<h1 align="caenter"> <%= "Category: " + @category.name %> </h1>

<div align="center">
  <div class="row" align="left" style = "padding-left: 20px; padding-bottom: 10px">
    <% if is_admin? %>
      <span class="badge">
        <%= link_to "Edit Category Name", edit_category_path(@category) %>
      </span>
    <% end %>
  </div>
  <%= will_paginate @category_articles %>
</div>

<%= render 'articles/articles_listing', articles: @category_articles %>

<div align="center">
  <%= will_paginate @category_articles %>
</div>
